import org.junit.Assert;
import org.junit.Test;

/**
 * Тестовый класс для InfiniteSeriesCalculator.
 */
public class TestInfiniteSeriesCalculator {

    @Test
    public void testCalculateSeries() {
//        long steps = 1000;  // уменьшено для ускорения тестирования
//        InfiniteSeriesCalculator calculator = new InfiniteSeriesCalculator(steps);
//        calculator.run();
//
//        double result = calculator.getResult();
//        Assert.assertTrue(result > 0);  // Проверка, что результат положителен
    }
}
